import { Activity, ArrowUpRight, Calendar, Check, Footprints, Github } from "lucide-react";
import { projects } from "../data/portfolio";

export default function Projects() {
  return <section id="projects" className="py-24"><div className="section-container"><div className="flex flex-col md:flex-row md:items-end md:justify-between gap-5 mb-14"><div><p className="text-primary-400 font-mono text-xs tracking-[.2em] uppercase mb-3">Selected work</p><h2 className="section-title">Projects with purpose.</h2></div><p className="text-dark-400 text-sm max-w-sm md:text-right">A look at the products I have designed and developed across frontend, backend, and data.</p></div><div className="grid lg:grid-cols-2 gap-6">{projects.map((project) => { const Icon = project.icon === "activity" ? Activity : Footprints; const isSky = project.tone === "sky"; return <article key={project.id} className="card card-hover p-7 flex flex-col group"><div className="flex items-start justify-between gap-4 mb-8"><div className={`w-12 h-12 rounded-2xl grid place-items-center border ${isSky ? "bg-primary-500/10 border-primary-500/20 text-primary-300" : "bg-amber-500/10 border-amber-500/20 text-amber-300"}`}><Icon size={22} strokeWidth={1.7} /></div><div className="flex items-center gap-1.5 text-xs text-dark-500"><Calendar size={12} />{project.date}</div></div><p className="text-xs uppercase tracking-wider text-dark-500 mb-2">{project.subtitle}</p><h3 className="text-2xl font-semibold text-white group-hover:text-primary-200 transition-colors mb-3">{project.title}</h3><p className="text-sm text-dark-400 leading-relaxed mb-6">{project.description}</p><ul className="space-y-2.5 mb-7 flex-1">{project.highlights.map((highlight) => <li key={highlight} className="flex gap-2 text-sm text-dark-400"><Check size={15} className="text-accent-400 mt-0.5 shrink-0" />{highlight}</li>)}</ul><div className="flex flex-wrap gap-2 mb-7">{project.tech.map((tech) => <span key={tech} className="tag bg-dark-800 text-dark-400 border border-dark-700">{tech}</span>)}</div><a href={project.github} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-medium text-dark-300 hover:text-primary-300 transition-colors">View on GitHub <Github size={15} /><ArrowUpRight size={14} className="opacity-60" /></a></article>})}</div></div></section>;
}
