import { Award, Calendar } from "lucide-react";
import { certificates } from "../data/portfolio";

export default function Certificates() {
  return <section id="certificates" className="py-24 relative"><div className="absolute inset-0 -z-10 bg-gradient-to-b from-transparent via-primary-950/20 to-transparent" /><div className="section-container"><div className="max-w-2xl mb-14"><p className="text-primary-400 font-mono text-xs tracking-[.2em] uppercase mb-3">Credentials</p><h2 className="section-title">Always learning.</h2><p className="text-dark-400">Formal learning across cloud AI, security, language, and computer science fundamentals.</p></div><div className="grid sm:grid-cols-2 gap-5">{certificates.map((certificate) => <article key={certificate.id} className="card card-hover p-6"><div className="flex items-start justify-between gap-4 mb-5"><span className="w-10 h-10 rounded-xl bg-primary-500/10 border border-primary-500/20 grid place-items-center text-primary-300"><Award size={18} /></span><span className="tag bg-dark-800 border border-dark-700 text-dark-400">{certificate.category}</span></div><h3 className="text-base leading-snug font-semibold text-white mb-3">{certificate.title}</h3><p className="text-sm text-dark-400 leading-relaxed mb-5">{certificate.description}</p><div className="pt-4 border-t border-dark-700/60 flex items-center justify-between text-xs"><span className="text-dark-300 font-medium">{certificate.issuer}</span><span className="flex items-center gap-1.5 text-dark-500"><Calendar size={12} />{certificate.date}</span></div></article>)}</div></div></section>;
}
